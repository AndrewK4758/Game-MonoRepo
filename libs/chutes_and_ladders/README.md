# ChutesAndLadders

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ChutesAndLadders` to build the library.

## Running unit tests

Run `nx test ChutesAndLadders` to execute the unit tests via [Jest](https://jestjs.io).
